function renderStats(period){var now=new Date();var filtered;if(period==='week'){var ws=getWeekStart(now);var we=new Date(ws);we.setDate(we.getDate()+7);filtered=Store.sessions.filter(function(s){var d=new Date(s.date);return d>=ws&&d<we;});}else{filtered=Store.sessions.filter(function(s){var d=new Date(s.date);return d.getMonth()===now.getMonth()&&d.getFullYear()===now.getFullYear();});}document.getElementById('s-sessions').textContent=filtered.length;document.getElementById('s-students').textContent=Store.students.length;document.getElementById('s-revenue').textContent=formatMoney(filtered.reduce(function(a,s){return a+(Number(s.fee)||0);},0));var el=document.getElementById('stats-list');if(!filtered.length){el.innerHTML='<p class="muted">Chưa có dữ liệu.</p>';}else{el.innerHTML=filtered.sort(function(a,b){return(a.date+a.start_time).localeCompare(b.date+b.start_time);}).map(function(s){return'<div class="s-item"><div class="s-item-info"><strong>'+s.lesson+'</strong><span>'+(s.student_name||'')+' · '+fmtDate(s.date)+' · '+s.start_time.slice(0,5)+'–'+s.end_time.slice(0,5)+' · '+formatMoney(s.fee)+'</span></div></div>';}).join('');}}
